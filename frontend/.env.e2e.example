# E2E テスト実行時の環境変数（playwright.config.ts が読み込む）
# シェル側で同キーを設定している場合はシェル側が優先される。

# ブラウザテストの接続先（フロントエンド Next.js）
PLAYWRIGHT_BASE_URL=http://localhost:3000

# バックエンド FastAPI の接続先（ヘルスチェック・デモシード投入・テスト内 API 呼び出し）
E2E_API_BASE_URL=http://localhost:8000
