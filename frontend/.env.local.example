# API の接続先（バックエンド FastAPI サーバー）
# 未設定の場合は http://localhost:8000 が使われる。
#
# ⚠️ NEXT_PUBLIC_* はクライアントバンドルに含まれるため、
#    Orcarouter API キー等の機密情報をここに置かないこと（CLAUDE.md §5.1）。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
